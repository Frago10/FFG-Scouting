node_modules
.next
out
docs
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# NOTE — Build artifacts that DO get committed (live at the repo root,
# served by GitHub Pages from master / (root)):
#   index.html, 404.html, index.txt, 404/, _next/, images/, CNAME, .nojekyll
# These are produced by `npm run deploy` and must stay tracked.
